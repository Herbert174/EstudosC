#include <stdio.h>

int main()
    {
    int a = 3;
    int b = 2;
    double pontos = a / (double)b; //(double) para fazer um cast na varievel e a converter em double
    printf("%f\n", pontos);
    }